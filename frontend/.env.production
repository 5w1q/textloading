# 生产构建默认值（可按 CI 覆盖）
# API：留空则浏览器请求同源 /auth、/tasks… —— Nginx 必须把这些路径反代到 FastAPI（见 deploy/nginx-hub-locations.snippet.conf）
# 若改为 VITE_API_BASE=/api，则 Nginx 使用 location /api/ { proxy_pass http://127.0.0.1:18180/; }
VITE_AB_ORIGIN=https://sayhi-ab.asia
VITE_USE_AB_LOGIN=1
